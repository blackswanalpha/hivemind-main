[package]
name = "hivemind-ai-provider"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
# Filled out in step 05. Empty deps for now keeps the crate isolated.
